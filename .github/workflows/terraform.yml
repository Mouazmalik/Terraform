name: Terraform Pipeline

on:
  push:
    branches:
      - main

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Code checkout
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Create credentials file
        run: echo '${{ secrets.GCP_CREDENTIALS }}' > key.json

      - name: Set env
        run: echo "GOOGLE_APPLICATION_CREDENTIALS=key.json" >> $GITHUB_ENV

      - name: Terraform Init
<<<<<<< HEAD
        run: $env:GODEBUG = "x509ignoreCN=1"
         terraform init
=======
        run: terraform init
>>>>>>> 44b30487a09d9c0c5f2390ebdc94f6612fa4bb72

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        run: terraform apply -auto-approve